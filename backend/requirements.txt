# Digitalisierung ABE — Python 3.13 Compatible Dependencies
# All packages pinned to versions with Python 3.13 + macOS ARM64 wheels

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Data validation
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Structured LLM output
instructor>=1.0.0

# File uploads
python-multipart>=0.0.17

# System monitoring — MUST be 6.1.0+ for Python 3.13 wheel
psutil>=6.1.0

# Environment variables
python-dotenv>=1.0.0

# Image processing — MUST be 11.0.0+ for Python 3.13 wheel
pillow>=11.0.0

# Numerical computing — MUST be 2.1.0+ for Python 3.13 wheel
numpy>=2.1.0

# Vector database
chromadb>=0.6.0

# Sentence embeddings
sentence-transformers>=3.3.0

# Apple Silicon MLX (auto-installed on macOS, skipped elsewhere)
mlx>=0.23.0; sys_platform == "darwin"
mlx-vlm>=0.1.14; sys_platform == "darwin"

# LLM inference via llama.cpp (auto-installed on macOS, skipped elsewhere)
llama-cpp-python>=0.3.5; sys_platform == "darwin"
pytesseract>=0.3.10
